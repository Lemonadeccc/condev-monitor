# Node example

这个目录目前是保留位，尚无 `package.json`、源码或可执行的 `dev` 脚本。因此根目录命令 `pnpm examples:node` 会明确说明该分组暂不可运行并以非零状态退出。

将来加入 Node 示例时，请在这里提供 `package.json` 与 `scripts.dev`，再把根 `package.json` 中的 `examples:node` 改成对应的 `pnpm --filter` 命令。
