{
    "name": "@condev-monitor/animation-lab-explorer",
    "version": "0.1.0",
    "private": false,
    "type": "module",
    "description": "Privacy-separated planning and Recorder-import contracts for reviewed animation lab scenarios",
    "main": "build/cjs/index.cjs",
    "module": "build/esm/index.js",
    "types": "build/types/index.d.ts",
    "exports": {
        ".": {
            "import": {
                "types": "./build/types/index.d.ts",
                "default": "./build/esm/index.js"
            },
            "require": {
                "types": "./build/types/index.d.ts",
                "default": "./build/cjs/index.cjs"
            }
        }
    },
    "files": [
        "build",
        "README.md"
    ],
    "publishConfig": {
        "access": "public"
    },
    "scripts": {
        "clean": "rimraf build",
        "build": "pnpm clean && pnpm build:transpile && pnpm build:types",
        "build:transpile": "tsup",
        "build:types": "tsc -p tsconfig.types.json",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "test": "pnpm build && node --test test/*.test.mjs"
    },
    "license": "Apache-2.0"
}
